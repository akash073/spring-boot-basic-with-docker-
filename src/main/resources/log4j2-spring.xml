<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%style{%d{ISO8601}}{black} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C{1.}}{bright,yellow}:  %logger{36}.%M\(%line\) %msg%n%throwable" />
        </Console>

        <!--  <RollingFile name="RollingFile"
                     fileName="@logging.location@/spring-boot-logger-log4j2.log"
                     filePattern="@logging.location@/$${date:yyyy-MM}/spring-boot-logger-log4j2-%d{-dd-MM-yyyy}-%i.log">-->

        <RollingFile name="RollingFile"
                     fileName="@logging.location@/spring-boot-logger-log4j2.log"
                     filePattern="@logging.location@/$${date:yyyy-MM}/spring-boot-logger-log4j2-%d{-dd-MM-yyyy}.log">
            <PatternLayout>
                <pattern>%d %p %C{1.} %logger{36}.%M\(%line\) - [%t] %m%n</pattern>
            </PatternLayout>
            <Policies>
                <!-- rollover on startup, daily and when the file reaches
                    10 MegaBytes -->
                <OnStartupTriggeringPolicy />
                <SizeBasedTriggeringPolicy
                        size="10 MB" />
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!--
        Level intLevel
        OFF   0
        FATAL 100
        ERROR 200
        WARN  300
        INFO  400
        DEBUG 500
        TRACE 600
        ALL   Integer.MAX_VALUE
        -->
        <!-- LOG everything at INFO level -->
        <Root level="info">
            <AppenderRef ref="Console" />
           <!-- <AppenderRef ref="RollingFile" />-->
        </Root>
    </Loggers>

</Configuration>